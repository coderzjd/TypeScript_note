// void类型是用于指定函数返回值的,大多数都是进行类型推断的
function fooA(num: number): void {}

// 可以将null 和 undefined 赋值给void
function foo(num: number): void {
  // return null
}
function bar(str: string): void {
  return undefined
}

fun
export {}
